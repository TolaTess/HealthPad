<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.business.AllDoctorsActivity">

    <include layout="@layout/app_bar_layout" android:id="@+id/all_doctors_toolbar"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/all_doctors_recycler"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/colorPrimary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/search_box_doctors"
        app:layout_constraintVertical_bias="0.0" />

    <SearchView
        android:id="@+id/search_box_doctors"
        android:layout_width="0dp"
        android:layout_height="35dp"
        android:layout_marginEnd="5dp"
        android:layout_marginStart="5dp"
        android:layoutDirection="rtl"
        android:textAlignment="center"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/all_doctors_toolbar" />
</androidx.constraintlayout.widget.ConstraintLayout>