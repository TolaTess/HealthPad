<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tolaotesanya.healthpad">

    <application
        android:name=".HealthPad"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.posts.PostsGalleryActivity"
            android:parentActivityName=".activities.MainActivity"/>
        <activity
            android:name=".activities.chat.ChatActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity android:name=".activities.posts.PostsActivity" />
        <activity
            android:name=".activities.doctorsprofile.DoctorProfileActivity"
            android:parentActivityName=".activities.doctors.AllDoctorsActivity" />
        <activity
            android:name=".activities.doctors.AllDoctorsActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity android:name=".activities.auth.DoctorRegisterActivity" />
        <activity
            android:name=".activities.accountsettings.SettingsActivity"
            android:parentActivityName=".activities.accountsettings.AccountActivity" />
        <activity
            android:name=".activities.accountsettings.AccountActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.auth.LoginActivity"
            android:parentActivityName=".activities.auth.AuthActivity" />
        <activity android:name=".activities.auth.AuthActivity" />
        <activity
            android:name=".activities.auth.RegisterActivity"
            android:parentActivityName=".activities.auth.AuthActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>